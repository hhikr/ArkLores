# v0.4 / v0.4.5 Task Breakdown

## Current Target

v0.4.5 收敛到中文 GameData 结构化知识库与 Summary Agent MVP。

## Completed

- ReAct Loop 支持宽松 Action Input 解析。
- ReAct Loop 处理空 final answer。
- ReAct Loop 读取 finish reason 并暴露截断错误。
- `search_local_lore` 改为 GameData 结构化检索。
- GameData builder 生成实体、别名、文档、原始记录、剧情行、片段与 FTS。
- App 支持下载 GameData release asset。
- GameData installer 在替换已安装 DB 前校验 schema、manifest 和基础计数。
- Query normalization 支持 `语音`、`档案`、`秘录`、`模组`、`肉鸽` 等常见中文意图词。
- Records/chunks fallback 支持分词 AND，改善 `集成战略 收藏品` 等组合查询。
- 内容类型意图会优先 records/chunks，避免 `敌人介绍`、`干员秘录` 被无关 entity document 或 medal 实体抢占。
- GameData builder 会为带中文逗号、括号、引号后缀的实体生成基础别名，改善 `特蕾西娅` 这类变体名消歧。
- `tools/check_gamedata_retrieval.dart` 可对完整 DB 跑固定检索 QA。
- 全项目 `flutter analyze` 已恢复到 no issues。
- 完整 GameData DB 已用当前 builder 重建，并生成 finalized manifest/report。
- 本地 release APK 已构建，注入未来 GitHub release asset URL 与当前 DB SHA。
- `tools/setup.sh` 参数模式可直接 dry-run/build，不会在远程 GameData URL 场景误报旧临时 HTTP 服务 pid。

## Phase 2: Structured RAG Cleanup

- Status: implemented.
- 旧 Wiki seed 运行路径已从 App 主线移除。
- 旧用户资料索引实现已移除。
- 旧引用详情查找实现已移除。
- 设置页只保留 Chat API。
- 知识库页只管理 GameData DB。
- Materials 页显示暂停态。

## Phase 3: Summary MVP

- Status: implemented for GameData-only MVP.
- Summary Agent 只调用 `search_local_lore`。
- 回答必须区分 GameData、Wiki、Book 的可信度。
- 当前无本地结果时说明知识库未覆盖。
- 引用信息优先使用 source path、raw id、content type。

## Phase 4: Verification

- Status: complete except user-deferred true-device E2E.
- Passed: `/home/hhikr/flutter/bin/flutter test test/agent_test.dart`
- Passed: `/home/hhikr/flutter/bin/flutter analyze`
- Passed: `tools/check_gamedata_retrieval.dart` fixed full-DB retrieval QA
- Passed: GameData schema smoke build with `--story-limit=1`
- Passed: schema smoke DB contains generated alias candidates for `特蕾西娅`
- Passed: rebuilt full DB contains generated alias candidates for `特蕾西娅`
- Passed: local release APK build with GameData URL/SHA dart defines
- Passed: `tools/setup.sh -a build -p android -m release --with-gamedata ... --dry-run`
- Deferred by user: 真机下载 GameData DB / 设备端 AI 对话 E2E。

## Remaining Before Release

- Complete true-device download/install/retrieval/Summary Agent E2E when user resumes device testing.
- Create final release notes.
- Create git tag.
- Create GitHub Release and upload APK + GameData assets.
