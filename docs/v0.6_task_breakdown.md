# v0.6 Task Breakdown

记录日期：2026-07-15。状态只反映本次实际实现与命令证据。

## Completed

- [x] GameData entity / alias 角色解析，开始前返回唯一 stable `entity_id` 或消歧候选。
- [x] Role-play Agent 只注册 `search_local_lore`，每轮至少一次工具调用。
- [x] 角色绑定工具强制 canonical entity id，并以角色档案、语音、秘录、模组和参与剧情为
  首轮记忆范围；未检索覆盖时禁止模型记忆补齐。
- [x] 用户场景标记为 session context，冲突、超出认知和无证据时间线受 prompt 约束。
- [x] 多轮历史、取消、重试、原子 JSON 本地存档、继续会话和重新开始。
- [x] UI 展示 canonical character、entity id、GameData 范围与生成对白非官方台词声明。
- [x] 新增用户可见文案进入中英文 ARB，并重新生成 localization 文件。
- [x] 单元测试覆盖唯一解析、重名、工具门槛、记忆约束、空/坏存档和保存/删除。
- [x] `/home/hhikr/flutter/bin/flutter test test/agent_test.dart`：41 passed。
- [x] `/home/hhikr/flutter/bin/flutter test test/fact_check_widget_test.dart`：2 passed，
  覆盖 fact-check 证据展开和 roleplay setup/conversation 窄屏渲染。
- [x] `ARKLORES_RUN_LIVE_CHAT=true /home/hhikr/flutter/bin/flutter test
  test/live_fact_check_test.dart`：3 passed。
- [x] `/home/hhikr/flutter/bin/flutter analyze`：No issues found。
- [x] finalized 完整 GameData DB retrieval QA：固定 query、`特蕾西娅` alias candidates 和
  `act21mini + 米格鲁 + 死亡` scoped evidence 通过。
- [x] schema smoke build：17680 entities、833 entity documents、1 story line、82930 lore chunks。
- [x] `tools/setup.sh` release GameData URL/SHA dry-run 参数解析通过。

## Deferred Verification

- [ ] finalized 完整 GameData DB 的多角色任务参与检索矩阵和低覆盖量化；本次仍未补足“多角色矩阵”这一更广的覆盖集。
- [ ] roleplay UI 仍缺少真实截图和真机渲染验收；自动 Widget 测试只能覆盖窄屏 Flutter 渲染。
- [ ] Android 真机上的本地存档恢复、双语、TalkBack、取消和长对话性能。
- [ ] tag、GitHub Release、远端 asset 上传和 push；本迭代未获发布授权。
