# v0.8 Task Breakdown

记录日期：2026-07-15。状态只反映本次实际实现与命令证据；版本尚未发布。

## Completed

- [x] Summary 与 Fact-check 统一 GameData-only 来源栏及发送/停止交互。
- [x] Summary 增加请求代次保护、取消、重试和本地化错误/取消状态，取消后的迟到响应不会写回 UI。
- [x] `search_local_lore` observation 使用独立字段 parser 转换为结构化证据记录；非 GameData
  和缺少必要 provenance 字段的 block 不展示为 GameData 证据。
- [x] Summary / Fact-check 的证据可展开显示 title、section、content type、source path、raw id、
  retrieval type、ranking reason、trust note 和原文片段。
- [x] 覆盖度只区分 scoped direct candidate 与 retrieved context，不把 retrieval score
  表述为事实确定性。
- [x] Agent 加载、推理、空、错误和取消文案进入中英文 ARB。
- [x] parser 单元测试覆盖多结果、边界格式与 unsupported source；Widget 测试覆盖窄屏、2x
  文字缩放、证据展开及 Summary 取消/重试。
- [x] 开发版本更新为 `0.8.0+8`。

## Deferred Verification

- [ ] Android 真机截图：常见手机尺寸、横竖屏、长回答、双语和极端文字缩放。
- [ ] TalkBack 的证据卡朗读顺序、展开状态与停止/重试按钮操作。
- [ ] 来源导航：当前 `source_path` 是 release DB provenance 路径，asset 不包含可打开的原始文件；
  在定义可信的源文件分发或外部仓库映射前不构造脆弱链接。
- [ ] 真实外部 Chat v0.8 专项 QA；本迭代没有修改 Agent prompt、ReAct 或 retrieval。
- [ ] tag、GitHub Release、APK 构建/上传及 push；本迭代未获发布授权。
