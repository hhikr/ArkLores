# v0.5 Task Breakdown

更新日期：2026-07-15

## 范围

本迭代实现 GameData-only 事实核查模式。唯一 Agent 工具是 `search_local_lore`；Wiki、
Book 和用户文本不进入证据源。为剧情取证增加 GameData schema v2/story scope builder
支持。不包含角色扮演和 Wiki 选中文字转交；版本收尾阶段经用户授权执行合并与发布。

## 已完成

- [x] 新增 Fact-Check Agent：拆解主张，分别检索实体、范围、支持证据与反证；使用 7 轮
  ReAct、4096 单步 token 预算，并要求最终回答前至少完成一次工具调用。
- [x] 结论使用 supported、refuted、uncertain、unavailable 四态协议。
- [x] supported / refuted 必须存在实际 GameData Result observation，否则强制降级。
- [x] prompt 区分直接证据、间接证据和证据缺失，不把 retrieval confidence 当事实确定性。
- [x] 多轮历史保留原主张与回答；话题切换必须显式说明并隔离旧证据。
- [x] UI 提供本地化四态标识、可展开 observation 引用、空状态、错误状态、取消和重试。
- [x] 单元测试覆盖工具限制、来源声明、无结果、实体歧义、结论解析和上下文追问。
- [x] 复用 Summary/ReAct 的 debug `AgentLogger`，日志标识 FactCheck，并在来源 guard 与
  verdict 校验后记录实际生效的最终结论；错误早退也会 flush。
- [x] Widget 测试覆盖窄屏、2 倍文字缩放和证据展开。
- [x] 删除 Warfarin crawler 的公网观测/正文打印测试，改为离线 parser 与 formatter 契约；
  bookmark model 测试按职责重命名。
- [x] Android 安装脚本与指南同步到 API 36；默认保留 App 数据，外部 GameData URL 默认
  强制 SHA256，并明确区分本地 release-mode 验收包与正式签名包。
- [x] GameData schema v2 增加通用 story scope 元数据；Fact-check evidence 模式按稳定
  scope ID、entity ID 与关系词交集检索剧情，普通档案和无结果不能放行确定 verdict。
- [x] 修复 provider 附加 Action metadata 时的工具名解析，以及 `handbook` 触发 Book
  source warning 的误报；固定完整 DB QA 强制使用绝对 DB 路径。
- [x] scoped evidence 候选按实体与关系词最短文本距离排序；无效 evidence 参数返回可重试
  指引；支持中文句末标点后紧跟 ReAct Action key。
- [x] 新增 opt-in 真实 Chat QA；API 只从 Git ignored 的 `tools/api_info` 读取，默认测试不
  发起外部请求。

## Deferred

- [ ] 使用真实外部 Chat API 执行 `RETRIEVAL_QA.md` 全部固定命题。已通过 scoped 剧情
  目标和无覆盖安全用例；其余固定命题及真机 UI/日志链路仍 deferred。
- [ ] Android 真机检查双主题、双语、TalkBack、取消时的供应商网络行为和长引用滚动。
  当前未连接设备。
- [ ] 合并、tag、GitHub Release 与 asset 上传在最终验证后执行并回读远端状态。

## 验证记录

- Passed: `/home/hhikr/flutter/bin/flutter test test/agent_test.dart`
- Passed: `/home/hhikr/flutter/bin/flutter test test/fact_check_widget_test.dart`
- Passed: `/home/hhikr/flutter/bin/flutter test`（47 passed，3 个 opt-in live tests skipped）
- Passed: `ARKLORES_RUN_LIVE_CHAT=true /home/hhikr/flutter/bin/flutter test
  test/live_fact_check_test.dart`（3 passed，使用 finalized 完整 DB）
- Passed: `/home/hhikr/flutter/bin/flutter analyze`
- Passed: `tools/setup.sh -a build -p android -m debug`（生成 debug APK）
- Passed: `tools/setup.sh -a build -p android -m release`（生成 debug-key 签名的本地验收 APK）
