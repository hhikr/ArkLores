# v0.7 Task Breakdown

记录日期：2026-07-15。状态只反映本次实际实现与命令证据。

## Completed

- [x] Wiki WebView 工具栏新增显式转交入口，可将当前选中文字、页面标题、URL 和站点标签
  包装为 `WikiAiContext`。
- [x] 转交目标支持 Summary 和 Fact-check；AI 页面根据目标打开对应 tab，并把 Wiki context
  作为用户消息提交给既有 Agent workflow。
- [x] Wiki context prompt 明确标记为 `not GameData evidence`，并要求使用
  `search_local_lore` 独立核验事实声明。
- [x] Summary / Fact-check Agent prompt 增加 v0.7 约束：Wiki reading context 只能用于解析
  实体、主张或问题，不能列为直接证据。
- [x] 新增用户可见文案进入中英文 ARB，并重新生成 localization 文件。
- [x] 单元测试覆盖有选区、空选区、source 声明和禁止伪造 GameData evidence。
- [x] Widget 测试覆盖 AI 页面接收 Wiki context 后进入 Summary 路径并渲染用户上下文。
- [x] `/home/hhikr/flutter/bin/flutter test test/agent_test.dart`：43 passed。
- [x] `/home/hhikr/flutter/bin/flutter test test/fact_check_widget_test.dart`：3 passed。
- [x] `/home/hhikr/flutter/bin/flutter analyze`：No issues found。
- [x] finalized 完整 DB retrieval QA：固定 query、`特蕾西娅` alias candidates 和
  `act21mini + 米格鲁 + 死亡` scoped evidence 通过。
- [x] v0.7.0 release-mode APK build：`ArkLores-0.7.0.apk`，SHA256
  `a656154d5cf0495fd70f12332b45da8ec29e2b7b9356e85bcb0e962c7ed96496`。
- [x] `tools/setup.sh` v0.7.0 release GameData URL/SHA dry-run 参数解析通过。
- [x] `apksigner verify --verbose --print-certs`：APK v1/v2 verified，Android Debug
  certificate 签名。

## Deferred Verification

- [ ] Android 真机 WebView 选中文字读取、底部面板交互、返回浏览页面和软键盘/系统选区行为。
- [ ] Fact-check Wiki context 的真实外部 Chat QA：需要确认模型会从 Wiki 文本解析主张后调用
  `search_local_lore`，GameData 无覆盖时不会直接认可 Wiki 文本。
- [ ] Summary Wiki context 的完整 DB 检索 QA：需要使用 finalized DB 验证常见 Wiki 页面标题、
  选中文字和实体歧义场景。
- [ ] 窄屏、双语、文字缩放和 TalkBack 下的 Wiki 转交底部面板截图或真机验收。
- [ ] 正式商店签名；当前 GitHub APK 仍是 Android Debug certificate 签名的验收包。
